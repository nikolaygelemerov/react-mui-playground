*** In order to start with `npm workspace` add in the root `package.json` file:

      "workspaces": [
        "apps/*",
        "packages/*"
        ]

*  "shared-ui": "*" | Add `./packages/shared-ui` in `./apps/mui-playground`
